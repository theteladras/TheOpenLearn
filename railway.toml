[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 300

# Runs before traffic switches — avoids blocking `node server.js` on DB during healthchecks.
[deploy.release]
command = "cd /app && prisma migrate deploy --schema prisma/schema.prisma"
